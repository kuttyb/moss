type Payload:
  value: Int

domain Worker:
  fn Read(payload: Payload) -> Int:
    reply payload.value

fn main():
  worker = Worker()
  var payload = Payload(value: 41)
  original_value = message worker.Read(payload)
  payload.value = 42
  echo original_value
  echo payload.value

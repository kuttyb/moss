type Holder:
  values: Vector [Int]

  fn replace(next: Vector [Int]):
    values = next

  fn installed_count() -> Int:
    return values |> count

fn main():
  holder = Holder(values: [1, 2])
  replacement = [3, 4, 5]
  holder.replace(replacement)
  echo holder.installed_count()

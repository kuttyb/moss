type Holder:
  values: Vector[Int]

  fn replace(next: Vector[Int]):
    values = next

fn main():
  holder = Holder(values: [1, 2])
  replacement = [3, 4, 5]
  holder.replace(replacement)
  echo replacement |> count

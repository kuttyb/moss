domain Worker
  on Maybe(should_reply: bool) -> bool
    if should_reply:
      reply true
    echo "falling through"

proc main()
  let worker = Worker()
  let result = message worker.Maybe(false)
  echo result

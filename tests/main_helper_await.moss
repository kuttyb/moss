fn wait_for(worker: Worker) -> int:
  value = message worker.Ping()
  return value

domain Worker:
  fn Ping() -> int:
    reply 9

fn main():
  worker = Worker()
  value = wait_for(worker)
  echo value

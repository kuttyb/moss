domain Catalog:
  title: string = "stable"

  fn Read() -> string:
    reply title

fn main():
  catalog = spawn Catalog()
  first = message catalog.Read()
  second = message catalog.Read()
  echo first, second

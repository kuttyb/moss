fn normalize(value: Int) -> Int:
  return value * 2

fn evaluate(values: Vector) -> Int:
  let normalized = values |> map(normalize)
  normalized |> filter(_ > 0) |> sum

proc main():
  values = [-2, 1, 3]
  echo evaluate(values)

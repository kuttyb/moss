domain Register:
  value: string = "initial"

  fn Set(next: string) -> string:
    value = next
    reply value

fn main():
  register = spawn Register()
  first = await register.Set("first")
  second = await register.Set("second")
  third = await register.Set("third")
  echo first, second, third

domain Register:
  value: string = "initial"

  fn Set() -> string:
    value = "set"
    reply value

fn main():
  register = spawn Register()
  first = message register.Set()
  second = message register.Set()
  third = message register.Set()
  echo first, second, third

domain Register:
  value: string = "initial"

  fn Set() -> string:
    value = "set"
    reply value

fn main():
  register = spawn Register()
  first = await register.Set()
  second = await register.Set()
  third = await register.Set()
  echo first, second, third

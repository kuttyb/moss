fn normalize(x):
  return x

domain Worker:
  payload

  fn Set(x):
    payload = normalize(x)

fn main():
  intWorker = spawn Worker()
  floatWorker = spawn Worker()
  message intWorker.Set(10)
  message floatWorker.Set(3.5)

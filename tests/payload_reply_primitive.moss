# Positive value-boundary regression under current synchronous semantics.
domain Worker:
  fn Echo(x: Int) -> Int:
    reply x

fn main():
  worker = Worker()
  result = message worker.Echo(1)
  echo result

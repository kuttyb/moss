domain Register:
  value = 7

  fn Swap(next: int) -> int:
    previous = value
    value = next
    reply previous

fn main():
  register = Register()
  previous = message register.Swap(9)
  echo previous

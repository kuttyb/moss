fn sum_to(limit: Int) -> Int:
  total = 0
  current = 1
  while current <= limit:
    total = total + current
    current = current + 1
  return total

fn main():
  assertEqual(sum_to(5), 15)
  echo sum_to(4)

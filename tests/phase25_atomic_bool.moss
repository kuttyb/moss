domain Flag:
  enabled = false

  fn Enable(value: bool) -> bool:
    enabled = value
    reply enabled

  fn Toggle() -> bool:
    enabled = not enabled
    reply enabled

fn main():
  flag = Flag()
  first = message flag.Enable(true)
  second = message flag.Toggle()
  echo first, second

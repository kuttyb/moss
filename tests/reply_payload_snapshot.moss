# Positive value-boundary regression under current synchronous semantics.
type Payload = object
  value: int

domain Worker
  on Build() -> Payload
    let payload = Payload(value: 7)
    reply payload

proc main()
  let worker = Worker()
  let payload = message worker.Build()
  echo payload.value

fn spin(value: Int) -> Int:
  while value < 0:
    let observed = value
  return value

proc main():
  values = [0, 1, 2]
  let inspected = values |> map(spin)
  item_count = inspected |> count
  echo item_count

domain Register:
  value: string = "ready"

  fn Read() -> string:
    reply value

domain Reader:
  fn Run(register: Register):
    first = await register.Read()
    second = await register.Read()
    echo first, second

fn main():
  register = spawn Register()
  left = spawn Reader()
  right = spawn Reader()
  message left.Run(register)
  message right.Run(register)

domain Worker:
  value: Int

  fn Set(x: Int):
    value = x
    echo "set"

  fn Get() -> Int:
    reply value

  fn Echo(x: Int) -> Int:
    reply x

fn main():
  worker = spawn Worker()
  message worker.Set(7)
  echo "next"
  result = message worker.Get()
  assertEqual(result, 7)
  echo message worker.Get()
  echoed = message worker.Echo(9)
  assertEqual(echoed, 9)

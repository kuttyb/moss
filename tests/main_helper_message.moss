fn wait_for(value: int) -> int:
  # Helpers receive ordinary results, not routing capabilities.
  return value

domain Worker:
  fn Ping() -> int:
    reply 9

fn main():
  worker = Worker()
  value = wait_for(message worker.Ping())
  echo value

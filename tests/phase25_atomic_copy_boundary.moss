type Token:
  label: string

domain Gate:
  enabled = false

  fn Remember(next: bool, token: Token) -> bool:
    enabled = next
    reply enabled

fn main():
  gate = spawn Gate()
  token = Token(label = "original")
  returned = message gate.Remember(true, token)
  echo token.label, returned

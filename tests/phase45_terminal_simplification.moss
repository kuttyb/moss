fn double(value: Int) -> Int:
  return value * 2

proc main():
  values = [1, 2, 3, 4]
  direct_count = values |> count
  mapped_count = values |> map(double) |> count
  echo direct_count, mapped_count

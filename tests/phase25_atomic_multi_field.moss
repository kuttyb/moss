domain Stats:
  count = 0
  enabled = false

  fn Increment() -> int:
    count = count + 1
    reply count

  fn Toggle() -> bool:
    enabled = not enabled
    reply enabled

fn main():
  stats = spawn Stats()
  count = await stats.Increment()
  enabled = await stats.Toggle()
  echo count, enabled

domain A:
  value

  fn Set(x):
    value = x

  fn Run() -> int:
    reply 1

domain A__helper:
  fn Run() -> int:
    reply 1

type Receiver:
  fn Send(worker: A):
    echo 1

fn main():
  helper = A()
  message helper.Set(10)
  other = A__helper()
  receiver = Receiver()
  receiver.Send(other)

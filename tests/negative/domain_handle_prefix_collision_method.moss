domain A:
  fn Run() -> int:
    reply 1

domain A__helper:
  fn Run() -> int:
    reply 1

type Receiver:
  fn Send(worker: A):
    echo 1

fn main():
  helper = spawn A__helper()
  receiver = Receiver()
  receiver.Send(helper)

domain Worker
  on Notify(text: string)
    echo text

domain Sender
  on Start(worker: Worker, text: string)
    message worker.Notify(text)

proc main()
  let worker = Worker()
  let sender = Sender()
  message sender.Start(worker, "fresh")

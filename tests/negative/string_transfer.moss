domain Worker
  on Notify(text: string)
    echo text

domain Sender
  domainroutes(worker: Worker)
  on Start(text: string)
    message worker.Notify(text)

proc main()
  let worker = Worker()
  let sender = Sender(worker: worker)
  message sender.Start("fresh")

domain Worker:
  fn Run():
    echo "run"

domain Router:
  domainroutes(worker: Worker)

  fn Reset():
    worker = worker

proc main():
  worker = Worker()
  router = Router(worker: worker)
  message router.Reset()

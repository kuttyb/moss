domain D:
  domainroutes(other: D)
  fn A():
    message other.B()

  fn B():
    echo "b"

# Check the declaration itself; a concrete recursive route universe would
# already be rejected as a cycle by composition checking.

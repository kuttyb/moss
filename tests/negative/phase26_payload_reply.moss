type Payload:
  value: Int

domain Worker:
  fn Echo(payload: Payload) -> Payload:
    reply payload

fn main():
  worker = Worker()
  data = Payload(value = 1)
  result = message worker.Echo(data)
  echo result.value

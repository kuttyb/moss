type Payload:
  value: Int

domain Worker:
  fn Echo(payload: Payload) -> Payload:
    reply payload

fn main():
  worker = spawn Worker()
  data = Payload(value = 1)
  result = await worker.Echo(data)
  echo result.value

type Buffer:
  value: Int

  fn clear():
    value = 0

domain Worker:
  fn Process(payload: Buffer):
    payload.clear()

fn main():
  worker = spawn Worker()
  buffer = Buffer(value = 1)
  message worker.Process(buffer)

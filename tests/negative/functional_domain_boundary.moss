domain Sink:
  fn Store(values: Vector):
    echo "stored"

fn double(value: Int) -> Int:
  return value * 2

proc main():
  sink = Sink()
  values = [1, 2, 3]
  message sink.Store(values |> map(double))

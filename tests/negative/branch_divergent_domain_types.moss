# Static-DAG migration: aliasing a handle is now rejected before branch joins.
domain Alpha:
  fn Ping() -> int:
    reply 1

domain Beta:
  fn Ping() -> int:
    reply 2

fn main():
  alpha = Alpha()
  beta = Beta()
  flag = true
  if flag:
    target = alpha
  else:
    target = beta

  echo 0

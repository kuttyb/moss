fn wait_if(flag: bool, worker: Worker) -> int:
  if flag:
    target = worker
  value = await target.Ping()
  return value

domain Worker:
  fn Ping() -> int:
    reply 1

fn main():
  worker = spawn Worker()
  value = wait_if(true, worker)
  echo value

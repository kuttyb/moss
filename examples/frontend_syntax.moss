# Modern frontend forms: inferred functions, colon-style value types, inferred domain
# state/replies, `=` constructors, pipelines, and synchronous messages.

type Quote:
  symbol
  price
  size
  ts

fn square(x: Int) = x * x

fn score(quote):
  value = quote.size |> square
  return value

domain Quotes:
  seen = 0

  fn Latest():
    seen = seen + 1
    reply Quote(symbol = "MOSS", price = 12.5, size = 2, ts = 100)

  fn Note(value: int):
    echo "note:", value

fn main():
  quotes = Quotes()
  quote = message quotes.Latest()
  result = score(quote)
  message quotes.Note(result)

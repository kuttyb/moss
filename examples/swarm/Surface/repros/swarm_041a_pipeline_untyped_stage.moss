type Square:
  side: Float

  fn area() -> Float:
    return side * side

fn area_of(s) -> Float:
  return s.area()

fn main():
  shapes = [Square(side: 1.0), Square(side: 2.0)]
  echo shapes |> map(area_of) |> sum

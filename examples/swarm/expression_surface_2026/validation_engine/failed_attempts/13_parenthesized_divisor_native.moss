type Rule:
  value: Int
  minimum: Int
  maximum: Int

  fn normalized():
    return (value - minimum) * 100 / (maximum - minimum)

  fn evaluate_at(configured_limit: Int) -> Evaluation:
    return Evaluation(normalized: (value - minimum) * 100 / (maximum - minimum), penalty: 0, accepted: value <= configured_limit, key: key)

  fn contribution() -> Int:
    return (value - minimum) * 100 / (maximum - minimum)

module vec2

export type Vec2:
  x: Float
  y: Float

  fn add(other: Vec2) -> Vec2:
    return Vec2(x: x + other.x, y: y + other.y)

  fn get_x() -> Float:
    return x

export fn vec(x: Float, y: Float) -> Vec2:
  return Vec2(x: x, y: y)

Natural downstream package: methods/fields on records' exported types, main without `module`.
Prereq: (cd ../../records && margo build)
`margo build` -> see REPORT. `margo debug` -> runs (source-based) except Fast Debug `for` limit.

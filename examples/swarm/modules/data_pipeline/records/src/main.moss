import orders
import validate

fn main():
  items = [orders.make_item("pen", 0, 12, 150), orders.make_item("ink", 0, 2, 900)]
  o = orders.make_order(1, 7, 0, items)
  echo "order", o.get_id(), o.total(), validate.order_ok(o), orders.region_name(o.get_region())

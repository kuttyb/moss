fn total_weight(items) -> Int:
  var t = 0
  for item in items:
    t = t + item.weight()
  return t

type Blob:
  size: Int

  fn weight():
    return size + 1

fn main():
  echo 1

test "generic total":
  blobs = [Blob(size: 1), Blob(size: 2)]
  assertEqual(total_weight(blobs), 5)

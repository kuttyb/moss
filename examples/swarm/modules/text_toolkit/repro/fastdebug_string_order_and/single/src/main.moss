fn smaller(a: String, b: String) -> String:
  if b < a:
    return "second"
  else:
    return "first"

fn in_range(x: Int) -> Bool:
  return x >= 1 and x < 9

fn main():
  echo smaller("pear", "apple")
  echo in_range(5)

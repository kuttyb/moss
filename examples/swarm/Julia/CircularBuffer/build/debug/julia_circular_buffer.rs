// Generated by Moss v0.1. Do not edit by hand.
// Synchronous domain calls enter compiler-planned handler-level 2PL.
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use std::collections::{HashMap, VecDeque};
use std::sync::Arc;
fn __moss_require_send<T: Send>() {}


struct MossAbortOnUnwind;
impl Drop for MossAbortOnUnwind {
    fn drop(&mut self) { if std::thread::panicking() { std::process::abort(); } }
}
#[cfg(any(test, moss_perf))]
static MOSS_LOCK_HOOK: std::sync::OnceLock<fn(&str, &str, &str, usize, usize, bool)> = std::sync::OnceLock::new();
#[inline]
fn moss_lock_event(event: &str, instance: &str, handler: &str, domain: usize, class: usize, exclusive: bool) {
    #[cfg(any(test, moss_perf))]
    if let Some(hook) = MOSS_LOCK_HOOK.get() { hook(event, instance, handler, domain, class, exclusive); }
}
#[inline]
fn moss_read_or_abort<T>(lock: &std::sync::RwLock<T>) -> std::sync::RwLockReadGuard<'_, T> {
    lock.read().unwrap_or_else(|_| std::process::abort())
}
#[inline]
fn moss_write_or_abort<T>(lock: &std::sync::RwLock<T>) -> std::sync::RwLockWriteGuard<'_, T> {
    lock.write().unwrap_or_else(|_| std::process::abort())
}
// Static object projections implement the private access trait used by owned
// objects too. Access capabilities are monomorphized, never selected at runtime.
// Checked effects make mutable access through a READ or absent projection
// unreachable; those trait methods fail closed if compiler invariants fail.
struct MossRead<'a, T>(&'a T);
struct MossWrite<'a, T>(&'a mut T);
struct MossAbsent<T>(std::marker::PhantomData<T>);
impl<T> std::ops::Deref for MossRead<'_, T> { type Target = T; fn deref(&self) -> &T { self.0 } }
impl<T> std::ops::DerefMut for MossRead<'_, T> { fn deref_mut(&mut self) -> &mut T { std::process::abort() } }
impl<T> std::ops::Deref for MossWrite<'_, T> { type Target = T; fn deref(&self) -> &T { self.0 } }
impl<T> std::ops::DerefMut for MossWrite<'_, T> { fn deref_mut(&mut self) -> &mut T { self.0 } }
impl<T> std::ops::Deref for MossAbsent<T> { type Target = T; fn deref(&self) -> &T { std::process::abort() } }
impl<T> std::ops::DerefMut for MossAbsent<T> { fn deref_mut(&mut self) -> &mut T { std::process::abort() } }
impl<T: std::fmt::Debug> std::fmt::Debug for MossRead<'_, T> { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { self.0.fmt(f) } }
impl<T: std::fmt::Debug> std::fmt::Debug for MossWrite<'_, T> { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { self.0.fmt(f) } }
impl<T> std::fmt::Debug for MossAbsent<T> { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { f.write_str("<unused>") } }
// Moss tooling begin|type|type:CircularBuffer@10|CircularBuffer|
// Moss line 10: type CircularBuffer:
// Moss backend: value representation for the Moss object; object ownership stays in Moss
#[derive(Clone, Debug)]
struct CircularBuffer {
    // Moss line 11: cap: Int
    cap: i64,
    // Moss line 12: start: Int
    start: i64,
    // Moss line 13: len: Int
    len: i64,
    // Moss line 14: allocated: Int
    allocated: i64,
    // Moss line 15: data: Vector [Int]
    data: Vec<i64>,
}

// Moss tooling end|type:CircularBuffer@10
impl CircularBuffer {
    fn __moss_into_parts(self) -> (i64,i64,i64,i64,Vec<i64>,) { (self.cap,self.start,self.len,self.allocated,self.data,) }
}
// Moss tooling begin|method|method:CircularBuffer.capacity@17|CircularBuffer::capacity|moss__method__CircularBuffer__capacity__6118d3377f6c241b
impl CircularBuffer {
    // Moss line 17: fn capacity
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__capacity__6118d3377f6c241b"]
    fn capacity(&self) -> i64 {
        // Moss line 18: return cap
        return self.cap;
    }
}

// Moss tooling end|method:CircularBuffer.capacity@17
// Moss tooling begin|method|method:CircularBuffer.length@20|CircularBuffer::length|moss__method__CircularBuffer__length__9678bfcdd4969df5
impl CircularBuffer {
    // Moss line 20: fn length
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__length__9678bfcdd4969df5"]
    fn length(&self) -> i64 {
        // Moss line 21: return len
        return self.len;
    }
}

// Moss tooling end|method:CircularBuffer.length@20
// Moss tooling begin|method|method:CircularBuffer.isempty@23|CircularBuffer::isempty|moss__method__CircularBuffer__isempty__299ba08240b0d1f1
impl CircularBuffer {
    // Moss line 23: fn isempty
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__isempty__299ba08240b0d1f1"]
    fn isempty(&self) -> bool {
        // Moss line 24: return len == 0
        return (self.len) == (0_i64);
    }
}

// Moss tooling end|method:CircularBuffer.isempty@23
// Moss tooling begin|method|method:CircularBuffer.isfull@26|CircularBuffer::isfull|moss__method__CircularBuffer__isfull__923040f94f02035c
impl CircularBuffer {
    // Moss line 26: fn isfull
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__isfull__923040f94f02035c"]
    fn isfull(&self) -> bool {
        // Moss line 27: return len == cap
        return (self.len) == (self.cap);
    }
}

// Moss tooling end|method:CircularBuffer.isfull@26
// Moss tooling begin|method|method:CircularBuffer.push@29|CircularBuffer::push|moss__method__CircularBuffer__push__c99a8d737fe24762
impl CircularBuffer {
    // Moss line 29: fn push
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__push__c99a8d737fe24762"]
    fn push(&mut self, mut value: i64) -> i64 {
        // Moss line 30: if len < cap:
        if (self.len) < (self.cap) {
            // Moss line 31: if allocated < cap:
            if (self.allocated) < (self.cap) {
                // Moss line 32: data.push(value)
                self.data.push(value);
                // Moss line 33: allocated = allocated + 1
                self.allocated = (self.allocated).wrapping_add(1_i64);
            } else {
                // Moss line 34: else:
                // Moss line 35: idx = start + len
                // Moss backend: LOCAL assignment: introduce an inferred Moss binding
                let mut idx = (self.start).wrapping_add(self.len);
                // Moss line 36: if idx >= cap:
                if (idx) >= (self.cap) {
                    // Moss line 37: idx = idx - cap
                    idx = (idx).wrapping_sub(self.cap);
                }
                // Moss line 38: data [idx] = value
                (self.data)[(idx) as usize] = value;
            }
            // Moss line 39: len = len + 1
            self.len = (self.len).wrapping_add(1_i64);
        } else {
            // Moss line 40: else:
            // Moss line 41: data [start] = value
            (self.data)[(self.start) as usize] = value;
            // Moss line 42: start = start + 1
            self.start = (self.start).wrapping_add(1_i64);
            // Moss line 43: if start == cap:
            if (self.start) == (self.cap) {
                // Moss line 44: start = 0
                self.start = 0_i64;
            }
        }
        // Moss line 45: return len
        return self.len;
    }
}

// Moss tooling end|method:CircularBuffer.push@29
// Moss tooling begin|method|method:CircularBuffer.pop@47|CircularBuffer::pop|moss__method__CircularBuffer__pop__0e94b8f199f998a1
impl CircularBuffer {
    // Moss line 47: fn pop
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__pop__0e94b8f199f998a1"]
    fn pop(&mut self) -> i64 {
        // Moss line 48: last_idx = start + len - 1
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut last_idx = ((self.start).wrapping_add(self.len)).wrapping_sub(1_i64);
        // Moss line 49: if last_idx >= cap:
        if (last_idx) >= (self.cap) {
            // Moss line 50: last_idx = last_idx - cap
            last_idx = (last_idx).wrapping_sub(self.cap);
        }
        // Moss line 51: result = data [last_idx]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut result = ((self.data)[(last_idx) as usize]).clone();
        // Moss line 52: len = len - 1
        self.len = (self.len).wrapping_sub(1_i64);
        // Moss line 53: return result
        return result;
    }
}

// Moss tooling end|method:CircularBuffer.pop@47
// Moss tooling begin|method|method:CircularBuffer.popfirst@55|CircularBuffer::popfirst|moss__method__CircularBuffer__popfirst__b4c3a1003c3d3f58
impl CircularBuffer {
    // Moss line 55: fn popfirst
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__popfirst__b4c3a1003c3d3f58"]
    fn popfirst(&mut self) -> i64 {
        // Moss line 56: result = data [start]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut result = ((self.data)[(self.start) as usize]).clone();
        // Moss line 57: start = start + 1
        self.start = (self.start).wrapping_add(1_i64);
        // Moss line 58: if start == cap:
        if (self.start) == (self.cap) {
            // Moss line 59: start = 0
            self.start = 0_i64;
        }
        // Moss line 60: len = len - 1
        self.len = (self.len).wrapping_sub(1_i64);
        // Moss line 61: return result
        return result;
    }
}

// Moss tooling end|method:CircularBuffer.popfirst@55
// Moss tooling begin|method|method:CircularBuffer.first@63|CircularBuffer::first|moss__method__CircularBuffer__first__6000d91d8dff7a44
impl CircularBuffer {
    // Moss line 63: fn first
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__first__6000d91d8dff7a44"]
    fn first(&self) -> i64 {
        // Moss line 64: return data [start]
        return ((self.data)[(self.start) as usize]).clone();
    }
}

// Moss tooling end|method:CircularBuffer.first@63
// Moss tooling begin|method|method:CircularBuffer.last@66|CircularBuffer::last|moss__method__CircularBuffer__last__c5de2f050f845873
impl CircularBuffer {
    // Moss line 66: fn last
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__last__c5de2f050f845873"]
    fn last(&self) -> i64 {
        // Moss line 67: last_idx = start + len - 1
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut last_idx = ((self.start).wrapping_add(self.len)).wrapping_sub(1_i64);
        // Moss line 68: if last_idx >= cap:
        if (last_idx) >= (self.cap) {
            // Moss line 69: last_idx = last_idx - cap
            last_idx = (last_idx).wrapping_sub(self.cap);
        }
        // Moss line 70: return data [last_idx]
        return ((self.data)[(last_idx) as usize]).clone();
    }
}

// Moss tooling end|method:CircularBuffer.last@66
// Moss tooling begin|method|method:CircularBuffer.get@72|CircularBuffer::get|moss__method__CircularBuffer__get__c825aca5b160989c
impl CircularBuffer {
    // Moss line 72: fn get
    #[inline(never)]
    #[export_name = "moss__method__CircularBuffer__get__c825aca5b160989c"]
    fn get(&self, mut index: i64) -> i64 {
        // Moss line 73: idx = start + index
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut idx = (self.start).wrapping_add(index);
        // Moss line 74: if idx >= cap:
        if (idx) >= (self.cap) {
            // Moss line 75: idx = idx - cap
            idx = (idx).wrapping_sub(self.cap);
        }
        // Moss line 76: return data [idx]
        return ((self.data)[(idx) as usize]).clone();
    }
}

// Moss tooling end|method:CircularBuffer.get@72
trait MossAccess_CircularBuffer: std::fmt::Debug {
    fn __moss_field_cap(&self) -> &i64;
    fn __moss_field_cap_mut(&mut self) -> &mut i64;
    fn __moss_field_start(&self) -> &i64;
    fn __moss_field_start_mut(&mut self) -> &mut i64;
    fn __moss_field_len(&self) -> &i64;
    fn __moss_field_len_mut(&mut self) -> &mut i64;
    fn __moss_field_allocated(&self) -> &i64;
    fn __moss_field_allocated_mut(&mut self) -> &mut i64;
    fn __moss_field_data(&self) -> &Vec<i64>;
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64>;
    fn __moss_value(&self) -> CircularBuffer { CircularBuffer { cap: self.__moss_field_cap().clone(), start: self.__moss_field_start().clone(), len: self.__moss_field_len().clone(), allocated: self.__moss_field_allocated().clone(), data: self.__moss_field_data().clone(), } }
    fn __moss_replace(&mut self, value: CircularBuffer) {
        *self.__moss_field_cap_mut() = value.cap;
        *self.__moss_field_start_mut() = value.start;
        *self.__moss_field_len_mut() = value.len;
        *self.__moss_field_allocated_mut() = value.allocated;
        *self.__moss_field_data_mut() = value.data;
    }
    // Moss tooling begin|method|method:CircularBuffer.capacity@17|MossAccess_CircularBuffer::capacity|
    // Moss line 17: fn capacity
    #[inline(never)]
    fn capacity(&self) -> i64 {
        // Moss line 18: return cap
        return *self.__moss_field_cap();
    }
    // Moss tooling end|method:CircularBuffer.capacity@17
    // Moss tooling begin|method|method:CircularBuffer.length@20|MossAccess_CircularBuffer::length|
    // Moss line 20: fn length
    #[inline(never)]
    fn length(&self) -> i64 {
        // Moss line 21: return len
        return *self.__moss_field_len();
    }
    // Moss tooling end|method:CircularBuffer.length@20
    // Moss tooling begin|method|method:CircularBuffer.isempty@23|MossAccess_CircularBuffer::isempty|
    // Moss line 23: fn isempty
    #[inline(never)]
    fn isempty(&self) -> bool {
        // Moss line 24: return len == 0
        return (*self.__moss_field_len()) == (0_i64);
    }
    // Moss tooling end|method:CircularBuffer.isempty@23
    // Moss tooling begin|method|method:CircularBuffer.isfull@26|MossAccess_CircularBuffer::isfull|
    // Moss line 26: fn isfull
    #[inline(never)]
    fn isfull(&self) -> bool {
        // Moss line 27: return len == cap
        return (*self.__moss_field_len()) == (*self.__moss_field_cap());
    }
    // Moss tooling end|method:CircularBuffer.isfull@26
    // Moss tooling begin|method|method:CircularBuffer.push@29|MossAccess_CircularBuffer::push|
    // Moss line 29: fn push
    #[inline(never)]
    fn push(&mut self, value: i64) -> i64 {
        // Moss line 30: if len < cap:
        if (*self.__moss_field_len()) < (*self.__moss_field_cap()) {
            // Moss line 31: if allocated < cap:
            if (*self.__moss_field_allocated()) < (*self.__moss_field_cap()) {
                // Moss line 32: data.push(value)
                (*self.__moss_field_data_mut()).push(value);
                // Moss line 33: allocated = allocated + 1
                *self.__moss_field_allocated_mut() = (*self.__moss_field_allocated()).wrapping_add(1_i64);
            } else {
                // Moss line 34: else:
                // Moss line 35: idx = start + len
                // Moss backend: LOCAL assignment: introduce an inferred Moss binding
                let mut idx = (*self.__moss_field_start()).wrapping_add(*self.__moss_field_len());
                // Moss line 36: if idx >= cap:
                if (idx) >= (*self.__moss_field_cap()) {
                    // Moss line 37: idx = idx - cap
                    idx = (idx).wrapping_sub(*self.__moss_field_cap());
                }
                // Moss line 38: data [idx] = value
                (*self.__moss_field_data_mut())[(idx) as usize] = value;
            }
            // Moss line 39: len = len + 1
            *self.__moss_field_len_mut() = (*self.__moss_field_len()).wrapping_add(1_i64);
        } else {
            // Moss line 40: else:
            // Moss line 41: data [start] = value
            (*self.__moss_field_data_mut())[(*self.__moss_field_start()) as usize] = value;
            // Moss line 42: start = start + 1
            *self.__moss_field_start_mut() = (*self.__moss_field_start()).wrapping_add(1_i64);
            // Moss line 43: if start == cap:
            if (*self.__moss_field_start()) == (*self.__moss_field_cap()) {
                // Moss line 44: start = 0
                *self.__moss_field_start_mut() = 0_i64;
            }
        }
        // Moss line 45: return len
        return *self.__moss_field_len();
    }
    // Moss tooling end|method:CircularBuffer.push@29
    // Moss tooling begin|method|method:CircularBuffer.pop@47|MossAccess_CircularBuffer::pop|
    // Moss line 47: fn pop
    #[inline(never)]
    fn pop(&mut self) -> i64 {
        // Moss line 48: last_idx = start + len - 1
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut last_idx = ((*self.__moss_field_start()).wrapping_add(*self.__moss_field_len())).wrapping_sub(1_i64);
        // Moss line 49: if last_idx >= cap:
        if (last_idx) >= (*self.__moss_field_cap()) {
            // Moss line 50: last_idx = last_idx - cap
            last_idx = (last_idx).wrapping_sub(*self.__moss_field_cap());
        }
        // Moss line 51: result = data [last_idx]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut result = (*self.__moss_field_data())[(last_idx) as usize];
        // Moss line 52: len = len - 1
        *self.__moss_field_len_mut() = (*self.__moss_field_len()).wrapping_sub(1_i64);
        // Moss line 53: return result
        return result;
    }
    // Moss tooling end|method:CircularBuffer.pop@47
    // Moss tooling begin|method|method:CircularBuffer.popfirst@55|MossAccess_CircularBuffer::popfirst|
    // Moss line 55: fn popfirst
    #[inline(never)]
    fn popfirst(&mut self) -> i64 {
        // Moss line 56: result = data [start]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut result = (*self.__moss_field_data())[(*self.__moss_field_start()) as usize];
        // Moss line 57: start = start + 1
        *self.__moss_field_start_mut() = (*self.__moss_field_start()).wrapping_add(1_i64);
        // Moss line 58: if start == cap:
        if (*self.__moss_field_start()) == (*self.__moss_field_cap()) {
            // Moss line 59: start = 0
            *self.__moss_field_start_mut() = 0_i64;
        }
        // Moss line 60: len = len - 1
        *self.__moss_field_len_mut() = (*self.__moss_field_len()).wrapping_sub(1_i64);
        // Moss line 61: return result
        return result;
    }
    // Moss tooling end|method:CircularBuffer.popfirst@55
    // Moss tooling begin|method|method:CircularBuffer.first@63|MossAccess_CircularBuffer::first|
    // Moss line 63: fn first
    #[inline(never)]
    fn first(&self) -> i64 {
        // Moss line 64: return data [start]
        return (*self.__moss_field_data())[(*self.__moss_field_start()) as usize];
    }
    // Moss tooling end|method:CircularBuffer.first@63
    // Moss tooling begin|method|method:CircularBuffer.last@66|MossAccess_CircularBuffer::last|
    // Moss line 66: fn last
    #[inline(never)]
    fn last(&self) -> i64 {
        // Moss line 67: last_idx = start + len - 1
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut last_idx = ((*self.__moss_field_start()).wrapping_add(*self.__moss_field_len())).wrapping_sub(1_i64);
        // Moss line 68: if last_idx >= cap:
        if (last_idx) >= (*self.__moss_field_cap()) {
            // Moss line 69: last_idx = last_idx - cap
            last_idx = (last_idx).wrapping_sub(*self.__moss_field_cap());
        }
        // Moss line 70: return data [last_idx]
        return (*self.__moss_field_data())[(last_idx) as usize];
    }
    // Moss tooling end|method:CircularBuffer.last@66
    // Moss tooling begin|method|method:CircularBuffer.get@72|MossAccess_CircularBuffer::get|
    // Moss line 72: fn get
    #[inline(never)]
    fn get(&self, index: i64) -> i64 {
        // Moss line 73: idx = start + index
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut idx = (*self.__moss_field_start()).wrapping_add(index);
        // Moss line 74: if idx >= cap:
        if (idx) >= (*self.__moss_field_cap()) {
            // Moss line 75: idx = idx - cap
            idx = (idx).wrapping_sub(*self.__moss_field_cap());
        }
        // Moss line 76: return data [idx]
        return (*self.__moss_field_data())[(idx) as usize];
    }
    // Moss tooling end|method:CircularBuffer.get@72
}
impl MossAccess_CircularBuffer for CircularBuffer {
    fn __moss_field_cap(&self) -> &i64 { &self.cap }
    fn __moss_field_cap_mut(&mut self) -> &mut i64 { &mut self.cap }
    fn __moss_field_start(&self) -> &i64 { &self.start }
    fn __moss_field_start_mut(&mut self) -> &mut i64 { &mut self.start }
    fn __moss_field_len(&self) -> &i64 { &self.len }
    fn __moss_field_len_mut(&mut self) -> &mut i64 { &mut self.len }
    fn __moss_field_allocated(&self) -> &i64 { &self.allocated }
    fn __moss_field_allocated_mut(&mut self) -> &mut i64 { &mut self.allocated }
    fn __moss_field_data(&self) -> &Vec<i64> { &self.data }
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64> { &mut self.data }
}
impl<T: MossAccess_CircularBuffer + ?Sized> MossAccess_CircularBuffer for &T {
    fn __moss_field_cap(&self) -> &i64 { (*self).__moss_field_cap() }
    fn __moss_field_cap_mut(&mut self) -> &mut i64 { std::process::abort() }
    fn __moss_field_start(&self) -> &i64 { (*self).__moss_field_start() }
    fn __moss_field_start_mut(&mut self) -> &mut i64 { std::process::abort() }
    fn __moss_field_len(&self) -> &i64 { (*self).__moss_field_len() }
    fn __moss_field_len_mut(&mut self) -> &mut i64 { std::process::abort() }
    fn __moss_field_allocated(&self) -> &i64 { (*self).__moss_field_allocated() }
    fn __moss_field_allocated_mut(&mut self) -> &mut i64 { std::process::abort() }
    fn __moss_field_data(&self) -> &Vec<i64> { (*self).__moss_field_data() }
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64> { std::process::abort() }
}
struct CircularBufferView<F0, F1, F2, F3, F4> {
    cap: F0,
    start: F1,
    len: F2,
    allocated: F3,
    data: F4,
}
impl<F0: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F1: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F2: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F3: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F4: std::ops::DerefMut<Target = Vec<i64>> + std::fmt::Debug> std::fmt::Debug for CircularBufferView<F0, F1, F2, F3, F4> {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("CircularBuffer").field("cap", &self.cap).field("start", &self.start).field("len", &self.len).field("allocated", &self.allocated).field("data", &self.data).finish()
    }
}
impl<F0: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F1: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F2: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F3: std::ops::DerefMut<Target = i64> + std::fmt::Debug, F4: std::ops::DerefMut<Target = Vec<i64>> + std::fmt::Debug> MossAccess_CircularBuffer for CircularBufferView<F0, F1, F2, F3, F4> {
    fn __moss_field_cap(&self) -> &i64 { &*self.cap }
    fn __moss_field_cap_mut(&mut self) -> &mut i64 { &mut *self.cap }
    fn __moss_field_start(&self) -> &i64 { &*self.start }
    fn __moss_field_start_mut(&mut self) -> &mut i64 { &mut *self.start }
    fn __moss_field_len(&self) -> &i64 { &*self.len }
    fn __moss_field_len_mut(&mut self) -> &mut i64 { &mut *self.len }
    fn __moss_field_allocated(&self) -> &i64 { &*self.allocated }
    fn __moss_field_allocated_mut(&mut self) -> &mut i64 { &mut *self.allocated }
    fn __moss_field_data(&self) -> &Vec<i64> { &*self.data }
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64> { &mut *self.data }
}

// Moss tooling begin|function|fn:buffer_capacity<CircularBuffer>@79|__moss_specialize_buffer_capacity_0|moss__function__buffer_capacity__43aeb9e2106e9689
// Moss line 79: fn buffer_capacity(buf: BoundedBuffer) -> Int:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
#[export_name = "moss__function__buffer_capacity__43aeb9e2106e9689"]
fn __moss_specialize_buffer_capacity_0(buf: &CircularBuffer) -> i64 {
    // Moss line 80: return buf.capacity()
    return buf.capacity();
}

// Moss tooling end|fn:buffer_capacity<CircularBuffer>@79
// Moss tooling begin|function|fn:buffer_capacity<CircularBuffer>@79|__moss_view___moss_specialize_buffer_capacity_0|
// Moss line 79: fn buffer_capacity(buf: BoundedBuffer) -> Int:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
fn __moss_view___moss_specialize_buffer_capacity_0(buf: &impl MossAccess_CircularBuffer) -> i64 {
    // Moss line 80: return buf.capacity()
    return (*buf).capacity();
}

// Moss tooling end|fn:buffer_capacity<CircularBuffer>@79
// Moss tooling begin|function|fn:buffer_length<CircularBuffer>@82|__moss_specialize_buffer_length_0|moss__function__buffer_length__c50ae805cb26b8bb
// Moss line 82: fn buffer_length(buf: BoundedBuffer) -> Int:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
#[export_name = "moss__function__buffer_length__c50ae805cb26b8bb"]
fn __moss_specialize_buffer_length_0(buf: &CircularBuffer) -> i64 {
    // Moss line 83: return buf.length()
    return buf.length();
}

// Moss tooling end|fn:buffer_length<CircularBuffer>@82
// Moss tooling begin|function|fn:buffer_length<CircularBuffer>@82|__moss_view___moss_specialize_buffer_length_0|
// Moss line 82: fn buffer_length(buf: BoundedBuffer) -> Int:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
fn __moss_view___moss_specialize_buffer_length_0(buf: &impl MossAccess_CircularBuffer) -> i64 {
    // Moss line 83: return buf.length()
    return (*buf).length();
}

// Moss tooling end|fn:buffer_length<CircularBuffer>@82
// Moss tooling begin|function|fn:buffer_is_full<CircularBuffer>@85|__moss_specialize_buffer_is_full_0|moss__function__buffer_is_full__503224ad918d8c22
// Moss line 85: fn buffer_is_full(buf: BoundedBuffer) -> Bool:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
#[export_name = "moss__function__buffer_is_full__503224ad918d8c22"]
fn __moss_specialize_buffer_is_full_0(buf: &CircularBuffer) -> bool {
    // Moss line 86: return buf.isfull()
    return buf.isfull();
}

// Moss tooling end|fn:buffer_is_full<CircularBuffer>@85
// Moss tooling begin|function|fn:buffer_is_full<CircularBuffer>@85|__moss_view___moss_specialize_buffer_is_full_0|
// Moss line 85: fn buffer_is_full(buf: BoundedBuffer) -> Bool:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
fn __moss_view___moss_specialize_buffer_is_full_0(buf: &impl MossAccess_CircularBuffer) -> bool {
    // Moss line 86: return buf.isfull()
    return (*buf).isfull();
}

// Moss tooling end|fn:buffer_is_full<CircularBuffer>@85
// Moss tooling begin|function|fn:available_space<CircularBuffer>@88|__moss_specialize_available_space_0|moss__function__available_space__c39b4045f0c81dde
// Moss line 88: fn available_space(buf: BoundedBuffer) -> Int:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
#[export_name = "moss__function__available_space__c39b4045f0c81dde"]
fn __moss_specialize_available_space_0(buf: &CircularBuffer) -> i64 {
    // Moss line 89: return buf.capacity() - buf.length()
    return (buf.capacity()).wrapping_sub(buf.length());
}

// Moss tooling end|fn:available_space<CircularBuffer>@88
// Moss tooling begin|function|fn:available_space<CircularBuffer>@88|__moss_view___moss_specialize_available_space_0|
// Moss line 88: fn available_space(buf: BoundedBuffer) -> Int:
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
fn __moss_view___moss_specialize_available_space_0(buf: &impl MossAccess_CircularBuffer) -> i64 {
    // Moss line 89: return buf.capacity() - buf.length()
    return ((*buf).capacity()).wrapping_sub((*buf).length());
}

// Moss tooling end|fn:available_space<CircularBuffer>@88
// Moss tooling begin|function|fn:inspect_capacity<CircularBuffer>@92|__moss_specialize_inspect_capacity_0|moss__function__inspect_capacity__987e93b10715a632
// Moss line 92: fn inspect_capacity(target):
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
#[export_name = "moss__function__inspect_capacity__987e93b10715a632"]
fn __moss_specialize_inspect_capacity_0(target: &CircularBuffer) -> i64 {
    // Moss line 93: return target.capacity()
    return target.capacity();
}

// Moss tooling end|fn:inspect_capacity<CircularBuffer>@92
// Moss tooling begin|function|fn:inspect_capacity<CircularBuffer>@92|__moss_view___moss_specialize_inspect_capacity_0|
// Moss line 92: fn inspect_capacity(target):
// Moss backend: STATIC specialization: concrete local function selected before Rust generation
#[inline(never)]
fn __moss_view___moss_specialize_inspect_capacity_0(target: &impl MossAccess_CircularBuffer) -> i64 {
    // Moss line 93: return target.capacity()
    return (*target).capacity();
}

// Moss tooling end|fn:inspect_capacity<CircularBuffer>@92
// Moss tooling begin|function|fn:circular_buffer@95|circular_buffer|moss__function__circular_buffer__cbf0a5715418d547
// Moss line 95: fn circular_buffer(capacity: Int) -> CircularBuffer:
// Moss backend: LOCAL function: ordinary intra-domain call; no domain dispatch
#[inline(never)]
#[export_name = "moss__function__circular_buffer__cbf0a5715418d547"]
fn circular_buffer(capacity: i64) -> CircularBuffer {
    // Moss line 96: return CircularBuffer(cap: capacity, start: 0, len: 0, allocated: 0, data: [])
    return CircularBuffer { cap: capacity, start: 0_i64, len: 0_i64, allocated: 0_i64, data: vec![] };
}

// Moss tooling end|fn:circular_buffer@95
// Moss tooling begin|main|main@231|main|moss__main
// Moss line 231: fn main():
// Moss backend: main entry point; domain calls below retain their statically selected lowering
#[inline(never)]
#[export_name = "moss__main"]
fn main() {
    // Moss line 232: cb = circular_buffer(3)
    // Moss backend: LOCAL assignment: introduce an inferred Moss binding
    let mut cb = circular_buffer(3_i64);
    // Moss line 233: cb.push(10)
    cb.push(10_i64);
    // Moss line 234: cb.push(20)
    cb.push(20_i64);
    // Moss line 235: cb.push(30)
    cb.push(30_i64);
    // Moss line 236: cb.push(40)
    cb.push(40_i64);
    // Moss line 237: echo cb.first(), cb.last(), cb.pop(), cb.pop()
    println!("{} {} {} {}", cb.first(), cb.last(), cb.pop(), cb.pop());
}
// Moss tooling end|main@231

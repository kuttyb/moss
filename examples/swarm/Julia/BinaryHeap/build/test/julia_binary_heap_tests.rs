// Generated by Moss v0.1. Do not edit by hand.
// Synchronous domain calls enter compiler-planned handler-level 2PL.
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(dead_code)]
#![allow(unused_imports)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use std::collections::{HashMap, VecDeque};
use std::sync::Arc;
fn __moss_require_send<T: Send>() {}


struct MossAbortOnUnwind;
impl Drop for MossAbortOnUnwind {
    fn drop(&mut self) { if std::thread::panicking() { std::process::abort(); } }
}
#[cfg(any(test, moss_perf))]
static MOSS_LOCK_HOOK: std::sync::OnceLock<fn(&str, &str, &str, usize, usize, bool)> = std::sync::OnceLock::new();
#[inline]
fn moss_lock_event(event: &str, instance: &str, handler: &str, domain: usize, class: usize, exclusive: bool) {
    #[cfg(any(test, moss_perf))]
    if let Some(hook) = MOSS_LOCK_HOOK.get() { hook(event, instance, handler, domain, class, exclusive); }
}
#[inline]
fn moss_read_or_abort<T>(lock: &std::sync::RwLock<T>) -> std::sync::RwLockReadGuard<'_, T> {
    lock.read().unwrap_or_else(|_| std::process::abort())
}
#[inline]
fn moss_write_or_abort<T>(lock: &std::sync::RwLock<T>) -> std::sync::RwLockWriteGuard<'_, T> {
    lock.write().unwrap_or_else(|_| std::process::abort())
}
// Static object projections implement the private access trait used by owned
// objects too. Access capabilities are monomorphized, never selected at runtime.
// Checked effects make mutable access through a READ or absent projection
// unreachable; those trait methods fail closed if compiler invariants fail.
struct MossRead<'a, T>(&'a T);
struct MossWrite<'a, T>(&'a mut T);
struct MossAbsent<T>(std::marker::PhantomData<T>);
impl<T> std::ops::Deref for MossRead<'_, T> { type Target = T; fn deref(&self) -> &T { self.0 } }
impl<T> std::ops::DerefMut for MossRead<'_, T> { fn deref_mut(&mut self) -> &mut T { std::process::abort() } }
impl<T> std::ops::Deref for MossWrite<'_, T> { type Target = T; fn deref(&self) -> &T { self.0 } }
impl<T> std::ops::DerefMut for MossWrite<'_, T> { fn deref_mut(&mut self) -> &mut T { self.0 } }
impl<T> std::ops::Deref for MossAbsent<T> { type Target = T; fn deref(&self) -> &T { std::process::abort() } }
impl<T> std::ops::DerefMut for MossAbsent<T> { fn deref_mut(&mut self) -> &mut T { std::process::abort() } }
impl<T: std::fmt::Debug> std::fmt::Debug for MossRead<'_, T> { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { self.0.fmt(f) } }
impl<T: std::fmt::Debug> std::fmt::Debug for MossWrite<'_, T> { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { self.0.fmt(f) } }
impl<T> std::fmt::Debug for MossAbsent<T> { fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result { f.write_str("<unused>") } }
// Moss tooling begin|type|type:BinaryHeap@3|BinaryHeap|
// Moss line 3: type BinaryHeap:
// Moss backend: value representation for the Moss object; object ownership stays in Moss
#[derive(Clone, Debug)]
struct BinaryHeap {
    // Moss line 4: data: Vector [Int]
    data: Vec<i64>,
}

// Moss tooling end|type:BinaryHeap@3
impl BinaryHeap {
    fn __moss_into_parts(self) -> (Vec<i64>,) { (self.data,) }
}
// Moss tooling begin|method|method:BinaryHeap.count@6|BinaryHeap::count|moss__method__BinaryHeap__count__1a5a7923a70cb5c6
impl BinaryHeap {
    // Moss line 6: fn count
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__count__1a5a7923a70cb5c6"]
    fn count(&self) -> i64 {
        // Moss line 7: return data |> count
        return {
        // Moss backend: EAGER FUNCTIONAL PIPELINE reference semantics (plan %1, semantic method:BinaryHeap.count@7:expression:0)
        // Moss functional origin|method:BinaryHeap.count@7:expression:0:source
        let __moss_pipeline_source = &(self.data);
        // Moss functional origin|method:BinaryHeap.count@7:expression:0:stage:1
        let mut __moss_result = 0_i64;
        for __moss_terminal_ref in __moss_pipeline_source.iter() {
            let __moss_terminal_value = *__moss_terminal_ref;
            __moss_result = __moss_result.wrapping_add(1_i64);
        }
        __moss_result
    };
    }
}

// Moss tooling end|method:BinaryHeap.count@6
// Moss tooling begin|method|method:BinaryHeap.empty@9|BinaryHeap::empty|moss__method__BinaryHeap__empty__2b6db94be8710161
impl BinaryHeap {
    // Moss line 9: fn empty
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__empty__2b6db94be8710161"]
    fn empty(&self) -> bool {
        // Moss line 10: return count() == 0
        return (self.count()) == (0_i64);
    }
}

// Moss tooling end|method:BinaryHeap.empty@9
// Moss tooling begin|method|method:BinaryHeap.peek@12|BinaryHeap::peek|moss__method__BinaryHeap__peek__c7b34c09b04cbb11
impl BinaryHeap {
    // Moss line 12: fn peek
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__peek__c7b34c09b04cbb11"]
    fn peek(&self) -> i64 {
        // Moss line 13: return data [0]
        return ((self.data)[(0_i64) as usize]).clone();
    }
}

// Moss tooling end|method:BinaryHeap.peek@12
// Moss tooling begin|method|method:BinaryHeap.percolate_up@16|BinaryHeap::percolate_up|moss__method__BinaryHeap__percolate_up__2205d1ac48dda5e1
impl BinaryHeap {
    // Moss line 16: fn percolate_up
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__percolate_up__2205d1ac48dda5e1"]
    fn percolate_up(&mut self, mut start: i64) -> i64 {
        // Moss line 17: value = data [start]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut value = ((self.data)[(start) as usize]).clone();
        // Moss line 18: position = start
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut position = start;
        // Moss line 19: while position > 0:
        while (position) > (0_i64) {
            // Moss line 20: parent = (position - 1) / 2
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut parent = (((position).wrapping_sub(1_i64))).wrapping_div(2_i64);
            // Moss line 21: parent_value = data [parent]
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut parent_value = ((self.data)[(parent) as usize]).clone();
            // Moss line 22: if parent_value <= value:
            if (parent_value) <= (value) {
                // Moss line 23: data [position] = value
                (self.data)[(position) as usize] = value;
                // Moss line 24: return position
                return position;
            }
            // Moss line 25: data [position] = parent_value
            (self.data)[(position) as usize] = parent_value;
            // Moss line 26: position = parent
            position = parent;
        }
        // Moss line 27: data [position] = value
        (self.data)[(position) as usize] = value;
        // Moss line 28: return position
        return position;
    }
}

// Moss tooling end|method:BinaryHeap.percolate_up@16
// Moss tooling begin|method|method:BinaryHeap.percolate_down@31|BinaryHeap::percolate_down|moss__method__BinaryHeap__percolate_down__602729cfbd468365
impl BinaryHeap {
    // Moss line 31: fn percolate_down
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__percolate_down__602729cfbd468365"]
    fn percolate_down(&mut self, mut start: i64) -> i64 {
        // Moss line 32: value = data [start]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut value = ((self.data)[(start) as usize]).clone();
        // Moss line 33: position = start
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut position = start;
        // Moss line 34: while position * 2 + 1 < count():
        while (((position).wrapping_mul(2_i64)).wrapping_add(1_i64)) < (self.count()) {
            // Moss line 35: left = position * 2 + 1
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut left = ((position).wrapping_mul(2_i64)).wrapping_add(1_i64);
            // Moss line 36: right = left + 1
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut right = (left).wrapping_add(1_i64);
            // Moss line 37: child = left
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut child = left;
            // Moss line 38: if right < count():
            if (right) < (self.count()) {
                // Moss line 39: if data [right] < data [left]:
                if (((self.data)[(right) as usize]).clone()) < (((self.data)[(left) as usize]).clone()) {
                    // Moss line 40: child = right
                    child = right;
                }
            }
            // Moss line 41: child_value = data [child]
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut child_value = ((self.data)[(child) as usize]).clone();
            // Moss line 42: if child_value >= value:
            if (child_value) >= (value) {
                // Moss line 43: data [position] = value
                (self.data)[(position) as usize] = value;
                // Moss line 44: return position
                return position;
            }
            // Moss line 45: data [position] = child_value
            (self.data)[(position) as usize] = child_value;
            // Moss line 46: position = child
            position = child;
        }
        // Moss line 47: data [position] = value
        (self.data)[(position) as usize] = value;
        // Moss line 48: return position
        return position;
    }
}

// Moss tooling end|method:BinaryHeap.percolate_down@31
// Moss tooling begin|method|method:BinaryHeap.push@50|BinaryHeap::push|moss__method__BinaryHeap__push__c055fe9c2d36898a
impl BinaryHeap {
    // Moss line 50: fn push
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__push__c055fe9c2d36898a"]
    fn push(&mut self, mut value: i64) -> i64 {
        // Moss line 51: data.push(value)
        self.data.push(value);
        // Moss line 52: percolate_up(count() - 1)
        self.percolate_up((self.count()) - (1_i64));
        // Moss line 53: return count()
        return self.count();
    }
}

// Moss tooling end|method:BinaryHeap.push@50
// Moss tooling begin|method|method:BinaryHeap.pop@55|BinaryHeap::pop|moss__method__BinaryHeap__pop__d4155d722a1964e6
impl BinaryHeap {
    // Moss line 55: fn pop
    #[inline(never)]
    #[export_name = "moss__method__BinaryHeap__pop__d4155d722a1964e6"]
    fn pop(&mut self) -> i64 {
        // Moss line 56: result = data [0]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut result = ((self.data)[(0_i64) as usize]).clone();
        // Moss line 57: last = count() - 1
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut last = (self.count()) - (1_i64);
        // Moss line 58: replacement = data [last]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut replacement = ((self.data)[(last) as usize]).clone();
        // Moss line 59: data.pop()
        self.data.pop();
        // Moss line 60: if count() > 0:
        if (self.count()) > (0_i64) {
            // Moss line 61: data [0] = replacement
            (self.data)[(0_i64) as usize] = replacement;
            // Moss line 62: percolate_down(0)
            self.percolate_down(0_i64);
        }
        // Moss line 63: return result
        return result;
    }
}

// Moss tooling end|method:BinaryHeap.pop@55
trait MossAccess_BinaryHeap: std::fmt::Debug {
    fn __moss_field_data(&self) -> &Vec<i64>;
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64>;
    fn __moss_value(&self) -> BinaryHeap { BinaryHeap { data: self.__moss_field_data().clone(), } }
    fn __moss_replace(&mut self, value: BinaryHeap) {
        *self.__moss_field_data_mut() = value.data;
    }
    // Moss tooling begin|method|method:BinaryHeap.count@6|MossAccess_BinaryHeap::count|
    // Moss line 6: fn count
    #[inline(never)]
    fn count(&self) -> i64 {
        // Moss line 7: return data |> count
        return {
        // Moss backend: EAGER FUNCTIONAL PIPELINE reference semantics (plan %1, semantic method:BinaryHeap.count@7:expression:0)
        // Moss functional origin|method:BinaryHeap.count@7:expression:0:source
        let __moss_pipeline_source = &(*self.__moss_field_data());
        // Moss functional origin|method:BinaryHeap.count@7:expression:0:stage:1
        let mut __moss_result = 0_i64;
        for __moss_terminal_ref in __moss_pipeline_source.iter() {
            let __moss_terminal_value = *__moss_terminal_ref;
            __moss_result = __moss_result.wrapping_add(1_i64);
        }
        __moss_result
    };
    }
    // Moss tooling end|method:BinaryHeap.count@6
    // Moss tooling begin|method|method:BinaryHeap.empty@9|MossAccess_BinaryHeap::empty|
    // Moss line 9: fn empty
    #[inline(never)]
    fn empty(&self) -> bool {
        // Moss line 10: return count() == 0
        return (self.count()) == (0_i64);
    }
    // Moss tooling end|method:BinaryHeap.empty@9
    // Moss tooling begin|method|method:BinaryHeap.peek@12|MossAccess_BinaryHeap::peek|
    // Moss line 12: fn peek
    #[inline(never)]
    fn peek(&self) -> i64 {
        // Moss line 13: return data [0]
        return (*self.__moss_field_data())[(0_i64) as usize];
    }
    // Moss tooling end|method:BinaryHeap.peek@12
    // Moss tooling begin|method|method:BinaryHeap.percolate_up@16|MossAccess_BinaryHeap::percolate_up|
    // Moss line 16: fn percolate_up
    #[inline(never)]
    fn percolate_up(&mut self, start: i64) -> i64 {
        // Moss line 17: value = data [start]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut value = (*self.__moss_field_data())[(start) as usize];
        // Moss line 18: position = start
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut position = start;
        // Moss line 19: while position > 0:
        while (position) > (0_i64) {
            // Moss line 20: parent = (position - 1) / 2
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut parent = (((position).wrapping_sub(1_i64))).wrapping_div(2_i64);
            // Moss line 21: parent_value = data [parent]
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut parent_value = (*self.__moss_field_data())[(parent) as usize];
            // Moss line 22: if parent_value <= value:
            if (parent_value) <= (value) {
                // Moss line 23: data [position] = value
                (*self.__moss_field_data_mut())[(position) as usize] = value;
                // Moss line 24: return position
                return position;
            }
            // Moss line 25: data [position] = parent_value
            (*self.__moss_field_data_mut())[(position) as usize] = parent_value;
            // Moss line 26: position = parent
            position = parent;
        }
        // Moss line 27: data [position] = value
        (*self.__moss_field_data_mut())[(position) as usize] = value;
        // Moss line 28: return position
        return position;
    }
    // Moss tooling end|method:BinaryHeap.percolate_up@16
    // Moss tooling begin|method|method:BinaryHeap.percolate_down@31|MossAccess_BinaryHeap::percolate_down|
    // Moss line 31: fn percolate_down
    #[inline(never)]
    fn percolate_down(&mut self, start: i64) -> i64 {
        // Moss line 32: value = data [start]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut value = (*self.__moss_field_data())[(start) as usize];
        // Moss line 33: position = start
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut position = start;
        // Moss line 34: while position * 2 + 1 < count():
        while (((position).wrapping_mul(2_i64)).wrapping_add(1_i64)) < (self.count()) {
            // Moss line 35: left = position * 2 + 1
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut left = ((position).wrapping_mul(2_i64)).wrapping_add(1_i64);
            // Moss line 36: right = left + 1
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut right = (left).wrapping_add(1_i64);
            // Moss line 37: child = left
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut child = left;
            // Moss line 38: if right < count():
            if (right) < (self.count()) {
                // Moss line 39: if data [right] < data [left]:
                if ((*self.__moss_field_data())[(right) as usize]) < ((*self.__moss_field_data())[(left) as usize]) {
                    // Moss line 40: child = right
                    child = right;
                }
            }
            // Moss line 41: child_value = data [child]
            // Moss backend: LOCAL assignment: introduce an inferred Moss binding
            let mut child_value = (*self.__moss_field_data())[(child) as usize];
            // Moss line 42: if child_value >= value:
            if (child_value) >= (value) {
                // Moss line 43: data [position] = value
                (*self.__moss_field_data_mut())[(position) as usize] = value;
                // Moss line 44: return position
                return position;
            }
            // Moss line 45: data [position] = child_value
            (*self.__moss_field_data_mut())[(position) as usize] = child_value;
            // Moss line 46: position = child
            position = child;
        }
        // Moss line 47: data [position] = value
        (*self.__moss_field_data_mut())[(position) as usize] = value;
        // Moss line 48: return position
        return position;
    }
    // Moss tooling end|method:BinaryHeap.percolate_down@31
    // Moss tooling begin|method|method:BinaryHeap.push@50|MossAccess_BinaryHeap::push|
    // Moss line 50: fn push
    #[inline(never)]
    fn push(&mut self, value: i64) -> i64 {
        // Moss line 51: data.push(value)
        (*self.__moss_field_data_mut()).push(value);
        // Moss line 52: percolate_up(count() - 1)
        self.percolate_up((self.count()) - (1_i64));
        // Moss line 53: return count()
        return self.count();
    }
    // Moss tooling end|method:BinaryHeap.push@50
    // Moss tooling begin|method|method:BinaryHeap.pop@55|MossAccess_BinaryHeap::pop|
    // Moss line 55: fn pop
    #[inline(never)]
    fn pop(&mut self) -> i64 {
        // Moss line 56: result = data [0]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut result = (*self.__moss_field_data())[(0_i64) as usize];
        // Moss line 57: last = count() - 1
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut last = (self.count()) - (1_i64);
        // Moss line 58: replacement = data [last]
        // Moss backend: LOCAL assignment: introduce an inferred Moss binding
        let mut replacement = (*self.__moss_field_data())[(last) as usize];
        // Moss line 59: data.pop()
        (*self.__moss_field_data_mut()).pop();
        // Moss line 60: if count() > 0:
        if (self.count()) > (0_i64) {
            // Moss line 61: data [0] = replacement
            (*self.__moss_field_data_mut())[(0_i64) as usize] = replacement;
            // Moss line 62: percolate_down(0)
            self.percolate_down(0_i64);
        }
        // Moss line 63: return result
        return result;
    }
    // Moss tooling end|method:BinaryHeap.pop@55
}
impl MossAccess_BinaryHeap for BinaryHeap {
    fn __moss_field_data(&self) -> &Vec<i64> { &self.data }
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64> { &mut self.data }
}
impl<T: MossAccess_BinaryHeap + ?Sized> MossAccess_BinaryHeap for &T {
    fn __moss_field_data(&self) -> &Vec<i64> { (*self).__moss_field_data() }
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64> { std::process::abort() }
}
struct BinaryHeapView<F0> {
    data: F0,
}
impl<F0: std::ops::DerefMut<Target = Vec<i64>> + std::fmt::Debug> std::fmt::Debug for BinaryHeapView<F0> {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("BinaryHeap").field("data", &self.data).finish()
    }
}
impl<F0: std::ops::DerefMut<Target = Vec<i64>> + std::fmt::Debug> MossAccess_BinaryHeap for BinaryHeapView<F0> {
    fn __moss_field_data(&self) -> &Vec<i64> { &*self.data }
    fn __moss_field_data_mut(&mut self) -> &mut Vec<i64> { &mut *self.data }
}

// Moss tooling begin|function|fn:empty_heap@65|empty_heap|moss__function__empty_heap__18b4cf540033be92
// Moss line 65: fn empty_heap() -> BinaryHeap:
// Moss backend: LOCAL function: ordinary intra-domain call; no domain dispatch
#[inline(never)]
#[export_name = "moss__function__empty_heap__18b4cf540033be92"]
fn empty_heap() -> BinaryHeap {
    // Moss line 66: return BinaryHeap(data: [])
    return BinaryHeap { data: vec![] };
}

// Moss tooling end|fn:empty_heap@65
fn __moss_protocol_hex(value: &str) -> String {
    let mut output = String::with_capacity(value.len() * 2);
    for byte in value.as_bytes() { use std::fmt::Write as _; let _ = write!(&mut output, "{:02x}", byte); }
    output
}

// Moss tooling begin|test|test:src/main.moss:orders the Julia BinaryHeap push and pop sequence|__moss_test_103d7d7772f3|
// Moss line 68: test "orders the Julia BinaryHeap push and pop sequence":
#[inline(never)]
fn __moss_test_103d7d7772f3() {
    // Moss line 69: heap = empty_heap()
    // Moss backend: LOCAL assignment: introduce an inferred Moss binding
    let mut heap = empty_heap();
    // Moss line 70: heap.push(5)
    heap.push(5_i64);
    // Moss line 71: heap.push(2)
    heap.push(2_i64);
    // Moss line 72: heap.push(7)
    heap.push(7_i64);
    // Moss line 73: heap.push(1)
    heap.push(1_i64);
    // Moss line 75: assertEqual(heap.peek(), 1)
    let __moss_assert_actual_0 = &(heap.peek());
    let __moss_assert_expected_0 = &(1_i64);
    if __moss_assert_actual_0 != __moss_assert_expected_0 { panic!("Moss assertion failed at line 75: assertEqual(heap.peek(), 1); actual={:?}, expected={:?}", __moss_assert_actual_0, __moss_assert_expected_0); }
    // Moss line 76: assertEqual(heap.pop(), 1)
    let __moss_assert_actual_1 = &(heap.pop());
    let __moss_assert_expected_1 = &(1_i64);
    if __moss_assert_actual_1 != __moss_assert_expected_1 { panic!("Moss assertion failed at line 76: assertEqual(heap.pop(), 1); actual={:?}, expected={:?}", __moss_assert_actual_1, __moss_assert_expected_1); }
    // Moss line 77: assertEqual(heap.pop(), 2)
    let __moss_assert_actual_2 = &(heap.pop());
    let __moss_assert_expected_2 = &(2_i64);
    if __moss_assert_actual_2 != __moss_assert_expected_2 { panic!("Moss assertion failed at line 77: assertEqual(heap.pop(), 2); actual={:?}, expected={:?}", __moss_assert_actual_2, __moss_assert_expected_2); }
    // Moss line 78: assertEqual(heap.pop(), 5)
    let __moss_assert_actual_3 = &(heap.pop());
    let __moss_assert_expected_3 = &(5_i64);
    if __moss_assert_actual_3 != __moss_assert_expected_3 { panic!("Moss assertion failed at line 78: assertEqual(heap.pop(), 5); actual={:?}, expected={:?}", __moss_assert_actual_3, __moss_assert_expected_3); }
    // Moss line 79: assertEqual(heap.pop(), 7)
    let __moss_assert_actual_4 = &(heap.pop());
    let __moss_assert_expected_4 = &(7_i64);
    if __moss_assert_actual_4 != __moss_assert_expected_4 { panic!("Moss assertion failed at line 79: assertEqual(heap.pop(), 7); actual={:?}, expected={:?}", __moss_assert_actual_4, __moss_assert_expected_4); }
    // Moss line 80: assert(heap.empty())
    if !(heap.empty()) { panic!("Moss assertion failed at line 80: assert(heap.empty())"); }
}
// Moss tooling end|test:src/main.moss:orders the Julia BinaryHeap push and pop sequence

// Moss tooling begin|test|test:src/main.moss:handles duplicates and negative values|__moss_test_c9468b3b554c|
// Moss line 82: test "handles duplicates and negative values":
#[inline(never)]
fn __moss_test_c9468b3b554c() {
    // Moss line 83: heap = empty_heap()
    // Moss backend: LOCAL assignment: introduce an inferred Moss binding
    let mut heap = empty_heap();
    // Moss line 84: heap.push(3)
    heap.push(3_i64);
    // Moss line 85: heap.push(-4)
    heap.push(-4_i64);
    // Moss line 86: heap.push(3)
    heap.push(3_i64);
    // Moss line 87: heap.push(-4)
    heap.push(-4_i64);
    // Moss line 89: assertEqual(heap.pop(), -4)
    let __moss_assert_actual_5 = &(heap.pop());
    let __moss_assert_expected_5 = &(-4_i64);
    if __moss_assert_actual_5 != __moss_assert_expected_5 { panic!("Moss assertion failed at line 89: assertEqual(heap.pop(), -4); actual={:?}, expected={:?}", __moss_assert_actual_5, __moss_assert_expected_5); }
    // Moss line 90: assertEqual(heap.pop(), -4)
    let __moss_assert_actual_6 = &(heap.pop());
    let __moss_assert_expected_6 = &(-4_i64);
    if __moss_assert_actual_6 != __moss_assert_expected_6 { panic!("Moss assertion failed at line 90: assertEqual(heap.pop(), -4); actual={:?}, expected={:?}", __moss_assert_actual_6, __moss_assert_expected_6); }
    // Moss line 91: assertEqual(heap.pop(), 3)
    let __moss_assert_actual_7 = &(heap.pop());
    let __moss_assert_expected_7 = &(3_i64);
    if __moss_assert_actual_7 != __moss_assert_expected_7 { panic!("Moss assertion failed at line 91: assertEqual(heap.pop(), 3); actual={:?}, expected={:?}", __moss_assert_actual_7, __moss_assert_expected_7); }
    // Moss line 92: assertEqual(heap.pop(), 3)
    let __moss_assert_actual_8 = &(heap.pop());
    let __moss_assert_expected_8 = &(3_i64);
    if __moss_assert_actual_8 != __moss_assert_expected_8 { panic!("Moss assertion failed at line 92: assertEqual(heap.pop(), 3); actual={:?}, expected={:?}", __moss_assert_actual_8, __moss_assert_expected_8); }
}
// Moss tooling end|test:src/main.moss:handles duplicates and negative values

// Moss tooling begin|test|test:src/main.moss:supports a single element and reuse after pop|__moss_test_c0e9bb947592|
// Moss line 94: test "supports a single element and reuse after pop":
#[inline(never)]
fn __moss_test_c0e9bb947592() {
    // Moss line 95: heap = empty_heap()
    // Moss backend: LOCAL assignment: introduce an inferred Moss binding
    let mut heap = empty_heap();
    // Moss line 96: heap.push(9)
    heap.push(9_i64);
    // Moss line 97: assertEqual(heap.peek(), 9)
    let __moss_assert_actual_9 = &(heap.peek());
    let __moss_assert_expected_9 = &(9_i64);
    if __moss_assert_actual_9 != __moss_assert_expected_9 { panic!("Moss assertion failed at line 97: assertEqual(heap.peek(), 9); actual={:?}, expected={:?}", __moss_assert_actual_9, __moss_assert_expected_9); }
    // Moss line 98: assertEqual(heap.pop(), 9)
    let __moss_assert_actual_10 = &(heap.pop());
    let __moss_assert_expected_10 = &(9_i64);
    if __moss_assert_actual_10 != __moss_assert_expected_10 { panic!("Moss assertion failed at line 98: assertEqual(heap.pop(), 9); actual={:?}, expected={:?}", __moss_assert_actual_10, __moss_assert_expected_10); }
    // Moss line 99: assert(heap.empty())
    if !(heap.empty()) { panic!("Moss assertion failed at line 99: assert(heap.empty())"); }
    // Moss line 101: heap.push(4)
    heap.push(4_i64);
    // Moss line 102: heap.push(-1)
    heap.push(-1_i64);
    // Moss line 103: assertEqual(heap.pop(), -1)
    let __moss_assert_actual_11 = &(heap.pop());
    let __moss_assert_expected_11 = &(-1_i64);
    if __moss_assert_actual_11 != __moss_assert_expected_11 { panic!("Moss assertion failed at line 103: assertEqual(heap.pop(), -1); actual={:?}, expected={:?}", __moss_assert_actual_11, __moss_assert_expected_11); }
    // Moss line 104: heap.push(2)
    heap.push(2_i64);
    // Moss line 105: assertEqual(heap.pop(), 2)
    let __moss_assert_actual_12 = &(heap.pop());
    let __moss_assert_expected_12 = &(2_i64);
    if __moss_assert_actual_12 != __moss_assert_expected_12 { panic!("Moss assertion failed at line 105: assertEqual(heap.pop(), 2); actual={:?}, expected={:?}", __moss_assert_actual_12, __moss_assert_expected_12); }
    // Moss line 106: assertEqual(heap.pop(), 4)
    let __moss_assert_actual_13 = &(heap.pop());
    let __moss_assert_expected_13 = &(4_i64);
    if __moss_assert_actual_13 != __moss_assert_expected_13 { panic!("Moss assertion failed at line 106: assertEqual(heap.pop(), 4); actual={:?}, expected={:?}", __moss_assert_actual_13, __moss_assert_expected_13); }
}
// Moss tooling end|test:src/main.moss:supports a single element and reuse after pop

// Moss tooling begin|test|test:src/main.moss:uses truncating zero-based parent division|__moss_test_ee1c5a6eff0b|
// Moss line 108: test "uses truncating zero-based parent division":
#[inline(never)]
fn __moss_test_ee1c5a6eff0b() {
    // Moss line 109: assertEqual((5 / 2), 2)
    let __moss_assert_actual_14 = &(((5_i64).wrapping_div(2_i64)));
    let __moss_assert_expected_14 = &(2_i64);
    if __moss_assert_actual_14 != __moss_assert_expected_14 { panic!("Moss assertion failed at line 109: assertEqual((5 / 2), 2); actual={:?}, expected={:?}", __moss_assert_actual_14, __moss_assert_expected_14); }
    // Moss line 110: assertEqual((7 - 1) / 2, 3)
    let __moss_assert_actual_15 = &((((7_i64).wrapping_sub(1_i64))).wrapping_div(2_i64));
    let __moss_assert_expected_15 = &(3_i64);
    if __moss_assert_actual_15 != __moss_assert_expected_15 { panic!("Moss assertion failed at line 110: assertEqual((7 - 1) / 2, 3); actual={:?}, expected={:?}", __moss_assert_actual_15, __moss_assert_expected_15); }
}
// Moss tooling end|test:src/main.moss:uses truncating zero-based parent division

fn main() {
    let previous_hook = std::panic::take_hook();
    std::panic::set_hook(Box::new(|_| {}));
    let mut failures = 0_usize;
    let outcome = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| __moss_test_103d7d7772f3()));
    match outcome {
        Ok(()) => println!("MOSS_TEST|PASS|{}|{}|", __moss_protocol_hex("test:src/main.moss:orders the Julia BinaryHeap push and pop sequence"), __moss_protocol_hex("orders the Julia BinaryHeap push and pop sequence")),
        Err(payload) => {
            failures += 1;
            let detail = if let Some(value) = payload.downcast_ref::<String>() { value.clone() } else if let Some(value) = payload.downcast_ref::<&str>() { (*value).to_string() } else { "test panicked without a Moss assertion message".to_string() };
            println!("MOSS_TEST|FAIL|{}|{}|{}", __moss_protocol_hex("test:src/main.moss:orders the Julia BinaryHeap push and pop sequence"), __moss_protocol_hex("orders the Julia BinaryHeap push and pop sequence"), __moss_protocol_hex(&detail));
        }
    }
    let outcome = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| __moss_test_c9468b3b554c()));
    match outcome {
        Ok(()) => println!("MOSS_TEST|PASS|{}|{}|", __moss_protocol_hex("test:src/main.moss:handles duplicates and negative values"), __moss_protocol_hex("handles duplicates and negative values")),
        Err(payload) => {
            failures += 1;
            let detail = if let Some(value) = payload.downcast_ref::<String>() { value.clone() } else if let Some(value) = payload.downcast_ref::<&str>() { (*value).to_string() } else { "test panicked without a Moss assertion message".to_string() };
            println!("MOSS_TEST|FAIL|{}|{}|{}", __moss_protocol_hex("test:src/main.moss:handles duplicates and negative values"), __moss_protocol_hex("handles duplicates and negative values"), __moss_protocol_hex(&detail));
        }
    }
    let outcome = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| __moss_test_c0e9bb947592()));
    match outcome {
        Ok(()) => println!("MOSS_TEST|PASS|{}|{}|", __moss_protocol_hex("test:src/main.moss:supports a single element and reuse after pop"), __moss_protocol_hex("supports a single element and reuse after pop")),
        Err(payload) => {
            failures += 1;
            let detail = if let Some(value) = payload.downcast_ref::<String>() { value.clone() } else if let Some(value) = payload.downcast_ref::<&str>() { (*value).to_string() } else { "test panicked without a Moss assertion message".to_string() };
            println!("MOSS_TEST|FAIL|{}|{}|{}", __moss_protocol_hex("test:src/main.moss:supports a single element and reuse after pop"), __moss_protocol_hex("supports a single element and reuse after pop"), __moss_protocol_hex(&detail));
        }
    }
    let outcome = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| __moss_test_ee1c5a6eff0b()));
    match outcome {
        Ok(()) => println!("MOSS_TEST|PASS|{}|{}|", __moss_protocol_hex("test:src/main.moss:uses truncating zero-based parent division"), __moss_protocol_hex("uses truncating zero-based parent division")),
        Err(payload) => {
            failures += 1;
            let detail = if let Some(value) = payload.downcast_ref::<String>() { value.clone() } else if let Some(value) = payload.downcast_ref::<&str>() { (*value).to_string() } else { "test panicked without a Moss assertion message".to_string() };
            println!("MOSS_TEST|FAIL|{}|{}|{}", __moss_protocol_hex("test:src/main.moss:uses truncating zero-based parent division"), __moss_protocol_hex("uses truncating zero-based parent division"), __moss_protocol_hex(&detail));
        }
    }
    std::panic::set_hook(previous_hook);
    if failures != 0 { std::process::exit(1); }
}

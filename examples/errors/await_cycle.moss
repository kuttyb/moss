# Historical Phase 2 fixture, retained as a negative migration example.
# Phase 10.6A retired source await; parsing rejects it before any topology analysis.

fn wait_for_worker(worker: Worker, coordinator: Coordinator) -> Int:
  let result = await worker.Check(coordinator)
  return result

domain Coordinator:
  fn Start(worker: Worker):
    if false:
      let result = wait_for_worker(worker, self)
      reply result
    reply 0

  fn Finish():
    reply 1

domain Worker:
  fn Check(coordinator: Coordinator):
    let result = await coordinator.Finish()
    reply result

fn main():
  let coordinator = Coordinator()
  let worker = Worker()
  message coordinator.Start(worker)
